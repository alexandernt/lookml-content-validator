Whenever you find [PERSONAL CREDENTIALS], it means that you need to replace it with your own credentials. 
JIC, u need to remove the bracketrs. For example: 
'[FILL WITH CREDENTIALS]' = 'credential_filled'
